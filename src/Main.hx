package ;

import js.Lib;

/**
 * ...
 * @author Tatsuro Ueda
 */

class Main {
	
	static function main() {
		var si = new ScrambledImage( "../bin/sample.jpg", { seed : 1, height : 8, width : 8 } );
		var f = new FizzBuzz();
		var r:Int = Rndm.integer( 10 );
	}
	
}